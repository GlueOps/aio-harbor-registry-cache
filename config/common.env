# Common environment variables shared across all environments
# These values are used unless overridden in environment-specific files

# Default admin password (should be overridden in environment-specific files)
export HARBOR_ADMIN_PASSWORD=Harbor12345

# Harbor and OpenTofu common settings
export HARBOR_USERNAME="admin"

# Certificate generation (can be overridden in environment-specific files)
export CREATE_LOCAL_CERTS=false
